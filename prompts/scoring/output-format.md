# Output

Return a single JSON object — no prose, no preamble, no markdown fence — with these fields:

```
{
  "hearts": 1 | 2 | 3,
  "axes_fired": [<2 or 3 axis names from: "specificity", "noticing", "non_obviousness", "groundedness", "generosity_of_frame">],
  "reason": "One sentence naming the salient axes. No emotional language."
}
```

Do not include any other field. Do not wrap the JSON in code fences. Do not add commentary before or after.
